# python_odrive_optitrack

This project is for synchronoizing the Odrive controll with optitrack capturing system. Main code is in `freq_sweep_odrive.py`, the rest are libraries derived from Natnet SDK.